#
# Enable taiclock services
#
# This is not a systemd preset.
# It goes in a special nosh system-manager preset directory.
# It is used by the nosh-run-taiclock package in order to enable the services themselves.
#

enable	taiclock.service
enable	cyclog@taiclock.service

# These services are incompatible.
disable	sntpclock.service
disable	cyclog@sntpclock.service

# fake-hwclock is not considered incompatible, as it is ordered before persistent-clock-loaded.target.
