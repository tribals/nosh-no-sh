#
# Explicitly enable basic services and targets.
#
# This is not a systemd preset.  
# It goes in a special nosh system-manager preset directory.
# It is used by the nosh-run-netbsd-{desktop,server}-base packages in order to enable certain mandatory basics without which a system simply won't work.
#

# Sysinit services
enable	devpubd-log.service
enable	devpubd.service
