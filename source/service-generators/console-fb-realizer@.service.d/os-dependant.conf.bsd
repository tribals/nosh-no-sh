## **************************************************************************
## For copyright and licensing terms, see the file named COPYING.
## **************************************************************************
# vim: set filetype=ini:

[Unit]
Description=User-space virtual terminal realizer on BSD framebuffer device %I

[Service]
ExecStartPre=sh -c 'test -c "/dev/%I" && chmod g+rw "/dev/%I"'
ExecRestartPre=sh -c 'test -c "/dev/%I" && chown :user-vt-realizer "/dev/%I"'
